# Internal Audit AI

BDO East Africa project folder 119. Lives on OneDrive, so avoid generating large build outputs inside the repo (they are gitignored, but OneDrive will still sync them).

## Status

New project. Stack and scope not yet decided. Update this file as decisions are made.

## Conventions

- Branch: `main`. Commit only when asked.
- Secrets go in `.env` (gitignored). Never commit credentials.
- Project notes and decisions live in `docs/`. Application code lives in `source/`.
- Client data is confidential: do not copy client files into the repo or into prompts unless the task requires it.

## Commands

_Add build, test and run commands here once the stack exists._
